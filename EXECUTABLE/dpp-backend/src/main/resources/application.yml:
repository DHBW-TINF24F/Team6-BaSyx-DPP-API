server:
  port: 8080

spring:
  data:
    mongodb:
      database: aas-env

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always