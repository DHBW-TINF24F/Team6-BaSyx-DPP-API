server:
  port: 8080

spring:
  application:
    name: dpp-backend
  data:
    mongodb:
      uri: mongodb://localhost:27017/dpp

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always